# GrassFlow 项目制作计划（修订版）

> 基于 26 项共识决策，TUI 优先，两周 MVP

---

## 项目架构

```
grassflow/                    # 单仓库 monorepo
├── core/                     # 共享核心（独立 Python 包）
│   ├── agent.py              # Agent 基类 + Schema 系统
│   ├── dag.py                # DAG 引擎 + 拓扑排序
│   ├── scheduler.py          # asyncio 并行调度器
│   ├── context.py            # 只读数据传递 Context
│   ├── monitor.py            # 监控 Agent（事后检查）
│   └── models.py             # 数据模型
│
├── tui/                      # TUI 入口（pip install grassflow）
│   ├── cli.py                # CLI 命令入口
│   ├── dsl_parser.py         # DSL 语法解析器
│   ├── runner.py             # 工作流执行器
│   └── display.py            # 终端进度展示（Rich）
│
├── server/                   # FastAPI 后端
│   ├── app.py                # FastAPI 应用
│   ├── api/
│   │   ├── workflows.py      # 工作流 CRUD API
│   │   └── executions.py     # 执行记录 API
│   └── ws.py                 # WebSocket 实时推送
│
├── gui/                      # Electron 前端（后续迭代）
│   ├── electron/             # Electron 壳
│   ├── src/                  # React + React Flow
│   └── package.json
│
├── examples/                 # 示例工作流 (.af 文件)
├── tests/                    # 测试
├── setup.py                  # Python 包配置
├── CLAUDE.md                 # 项目说明
└── 项目制作计划.md
```

---

## 核心设计决策

### Agent 定义

```python
class Agent:
    """所有 Agent 的基类"""
    name: str
    input_schema: dict        # JSON Schema
    output_schema: dict       # JSON Schema
    model: str = "gpt-4"
    prompt: str = ""
    on_fail: str = "stop"     # stop / skip / retry
    retry_count: int = 3

    async def run(self, input_data: dict) -> dict:
        """子类实现具体逻辑"""
        raise NotImplementedError
```

### 预设积木类型（后续 GUI 使用）

| 类别 | 积木 | 说明 |
|------|------|------|
| AI 类（蓝色） | 🤖 LLM Agent | 调用大模型 |
| | 🔍 Search Agent | 搜索网络/知识库 |
| | 📝 Writer Agent | 专门写作 |
| | 📊 Analyzer Agent | 数据分析 |
| 控制类（橙色） | 🔀 Condition | 条件分支 |
| | ⏳ Immediate | 立即执行 |
| | ⏸️ Human | 人工审批暂停点 |
| IO 类（绿色） | 📥 Input | 工作流输入 |
| | 📤 Output | 工作流输出 |

### DSL 语法规范

```grassflow
# 基础顺序
A -> B -> C

# 并行执行
(A, B, C) -> D

# 立即执行（先启动，遇依赖等待）
A | B

# 条件分支（ConditionAgent 输出 route 字段）
route -> [urgent] human, [normal] bot

# 组合使用
(A | B) -> route -> [urgent] human, [normal] bot

# 完整工作流定义
workflow ticket_processing {
  # Agent 声明
  agent classify {
    model: "gpt-4"
    prompt: "分类工单: {input}"
    input_schema: { "ticket": "string" }
    output_schema: { "category": "string" }
  }
  agent priority {
    model: "gpt-4"
    prompt: "判断优先级: {input}"
  }
  agent route {
    type: "condition"
    rules: ["urgent", "normal", "info"]
  }
  agent human { type: "manual" }
  agent bot {
    model: "gpt-4"
    prompt: "自动回复: {input}"
  }

  # 执行流
  classify | priority
  -> route
  -> [urgent] human, [normal] bot
}
```

### 数据传递机制

```python
class WorkflowContext:
    """只读数据传递"""
    def set(self, agent_id: str, data: dict):
        """Agent 只能写自己的 key"""
        self._data[agent_id] = data

    def get(self, agent_id: str) -> dict:
        """可以读任何 Agent 的输出"""
        return self._data.get(agent_id, {})

    def get_dependency_data(self, agent: Agent) -> dict:
        """调度器注入依赖数据到 Agent 输入"""
        deps = {}
        for dep_id in agent.dependencies:
            deps[dep_id] = self.get(dep_id)
        return {**agent.base_input, "_deps": deps}
```

### 失败策略

```python
# Agent 配置
on_fail: "stop"      # 默认：任何失败，整个工作流停止
on_fail: "skip"      # 跳过该 Agent，用空结果继续
on_fail: "retry"     # 重试，配合 retry_count
```

### 连线类型（GUI 后续迭代）

| 线型 | 含义 | 样式 |
|------|------|------|
| 实线 | 顺序依赖 | ━━━━━ |
| 虚线 | 立即执行 | ╌╌╌╌╌ |
| 粗线 | 条件分支 | ═════ |

### 数据持久化

```
~/.grassflow/
├── config.json               # 全局配置（API Key、默认模型）
├── workflows/                # 工作流定义（JSON）
│   ├── ticket_processing.json
│   └── competitor_analysis.json
└── grassflow.db              # 执行记录（SQLite）
```

### Schema 定义方式

| 场景 | 方式 |
|------|------|
| 代码定义 Agent | Python dict |
| GUI 编辑 | 表单 |
| 存储 | JSON Schema |

三种方式最终统一为 JSON Schema。

### 监控 Agent（事后监控）

```python
class MonitorAgent(Agent):
    """工作流执行完毕后检查结果"""
    async def run(self, execution_result):
        report = {
            "schema_violations": self.check_schemas(execution_result),
            "quality_issues": self.check_quality(execution_result),
            "timeout_warnings": self.check_timeouts(execution_result),
        }
        return report
```

### 配置管理

```yaml
# ~/.grassflow/config.json
{
  "default_model": "gpt-4",
  "api_keys": {
    "openai": "sk-xxx",
    "anthropic": "sk-xxx"
  }
}

# Agent 可选覆盖
# workflow.json 中 agent 配置
{
  "name": "special_agent",
  "model": "claude-sonnet-4-20250514",
  "api_key": "sk-xxx"    # 可选，不填用全局
}
```

---

## 两周开发计划（修订版）

### 第一阶段：项目骨架（Day 1-2，14h）

| 任务 | 时间 | 产出 |
|------|------|------|
| 初始化项目结构 | 2h | monorepo 骨架 |
| Agent 基类 + Schema 系统 | 4h | agent.py |
| WorkflowContext 只读传递 | 2h | context.py |
| 数据模型定义 | 3h | models.py |
| 配置管理（config.json 读写） | 3h | config.py |

**验收标准**：
- [ ] `Agent` 基类可实例化
- [ ] Schema 校验可用
- [ ] Context 数据传递可用
- [ ] 配置文件可读写

---

### 第二阶段：DAG 引擎（Day 3-4，14h）

| 任务 | 时间 | 产出 |
|------|------|------|
| DAG 拓扑排序算法 | 4h | dag.py |
| 依赖解析（顺序/并行） | 3h | dag.py |
| asyncio 并行调度器 | 4h | scheduler.py |
| 单元测试 | 3h | tests/ |

**验收标准**：
- [ ] `(A, B) -> C` 可正确并行执行 A、B，完成后执行 C
- [ ] `A -> B -> C` 可正确串行执行
- [ ] 拓扑排序支持环检测（报错）
- [ ] 单元测试覆盖核心逻辑

---

### 第三阶段：DSL 解析 + 条件分支（Day 5-6，14h）

| 任务 | 时间 | 产出 |
|------|------|------|
| DSL 语法设计定稿 | 2h | 语法规范 |
| DSL 解析器实现 | 6h | dsl_parser.py |
| ConditionAgent 实现 | 3h | condition.py |
| 条件分支调度支持 | 3h | scheduler.py 更新 |

**验收标准**：
- [ ] `A -> B -> C` 可解析为 DAG
- [ ] `(A, B) -> C` 可解析为并行依赖
- [ ] `A | B` 可解析为立即执行
- [ ] `route -> [x] A, [y] B` 可解析为条件分支
- [ ] ConditionAgent 可根据输出选择路由

---

### 第四阶段：LLM 集成（Day 7-8，14h）

| 任务 | 时间 | 产出 |
|------|------|------|
| LLM API 调用封装（LiteLLM） | 4h | llm.py |
| LLM Agent 实现 | 3h | llm_agent.py |
| TUI 命令行入口 | 3h | cli.py |
| 端到端联调 | 4h | 可运行的 TUI |

**验收标准**：
- [ ] `grassflow run workflow.af` 可执行工作流
- [ ] Agent 实际调用 LLM API
- [ ] 结果正确传递给下游 Agent
- [ ] 条件分支根据 LLM 输出正确路由

---

### 第五阶段：持久化 + 进度展示（Day 9-10，14h）

| 任务 | 时间 | 产出 |
|------|------|------|
| 工作流保存/加载（JSON） | 4h | storage.py |
| SQLite 执行记录 | 3h | db.py |
| 终端进度展示（Rich） | 4h | display.py |
| CLI 命令完善 | 3h | cli.py |

**验收标准**：
- [ ] `grassflow save workflow.af` 可保存工作流
- [ ] `grassflow list` 可列出已保存工作流
- [ ] 执行过程中终端显示进度条
- [ ] 执行记录可查询

---

### 第六阶段：监控 + 测试（Day 11-12，14h）

| 任务 | 时间 | 产出 |
|------|------|------|
| 监控 Agent（事后检查） | 5h | monitor.py |
| 执行报告生成 | 3h | report.py |
| 集成测试 | 4h | tests/ |
| Bug 修复 | 2h | - |

**验收标准**：
- [ ] 工作流执行完毕后生成监控报告
- [ ] 报告包含：Schema 校验、质量检查、耗时统计
- [ ] 核心功能有集成测试覆盖

---

### 第七阶段：打磨 + 文档（Day 13-14，10h）

| 任务 | 时间 | 产出 |
|------|------|------|
| Bug 修复 | 3h | - |
| 示例工作流编写 | 3h | examples/ |
| README + 文档 | 2h | docs/ |
| 演示准备 | 2h | - |

**验收标准**：
- [ ] 至少 2 个完整示例工作流
- [ ] README 包含安装和使用说明
- [ ] 可流畅演示完整流程

---

## MVP 功能清单（最终版）

### 必须实现（P0）— TUI 全流程闭环

- [ ] Agent 基类 + Schema 系统
- [ ] DAG 拓扑排序 + 并行调度
- [ ] DSL 解析器（顺序/并行/条件/立即执行）
- [ ] ConditionAgent（条件分支）
- [ ] LLM Agent + API 调用
- [ ] 只读数据传递（Context）
- [ ] 可配置失败策略（stop/skip/retry）
- [ ] 工作流保存/加载（JSON）
- [ ] 终端进度展示（Rich）
- [ ] CLI 入口（`grassflow run/list/save`）
- [ ] 监控报告（事后检查）
- [ ] 执行记录（SQLite）
- [ ] 至少 2 个示例工作流

### 后续迭代（P1）— GUI

- [ ] Electron 桌面应用
- [ ] React Flow 画布
- [ ] 左侧积木面板（拖拽式）
- [ ] 多种连线类型（实线/虚线/粗线）
- [ ] 右侧配置面板
- [ ] 实时状态推送（WebSocket）
- [ ] GUI/TUI 互通

### 不做（P2）

- [ ] 用户认证/多租户
- [ ] 自动布局
- [ ] 子图嵌套
- [ ] Loop 循环节点
- [ ] 生产级性能优化
- [ ] 模板市场

---

## 风险与应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| DSL 解析器实现复杂 | 中 | 高 | 先支持最简语法，迭代扩展 |
| LLM API 限流/费用 | 中 | 中 | 支持本地模型 Ollama |
| asyncio 调度边界情况 | 低 | 中 | 单元测试覆盖，MVP 阶段"任何失败则全部停止" |
| 两周时间不够 | 中 | 高 | 严格控制 P0 范围，GUI 全部推迟 |

---

## 每日检查点

| 天数 | 检查点 |
|------|--------|
| Day 2 | Agent 基类可用，Schema 校验通过 |
| Day 4 | DAG 调度可用，并行执行正常 |
| Day 6 | DSL 解析可用，条件分支正常 |
| Day 8 | TUI 可运行完整工作流，LLM 实际调用 |
| Day 10 | 工作流可保存/加载，终端有进度展示 |
| Day 12 | 监控报告可用，测试通过 |
| Day 14 | 示例工作流可演示，文档齐全 |
